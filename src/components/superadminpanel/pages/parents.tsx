import React from "react";
import { EntityPage } from "./pagekit";
import { parents, SCHOOL_NAMES } from "./mockData";
export default function ParentsPage({ onNavigate }: { onNavigate?: (page: string) => void }) { return <EntityPage title="Parents" subtitle="Search parent, child, school, bus and subscription access." seed={parents} schoolNames={SCHOOL_NAMES} onNavigate={onNavigate} metrics={[{ label: "Total parents", value: parents.length, icon: "people", color: "#7C3AED", note: "Across all schools" }, { label: "Active parents", value: parents.filter((p) => p.status === "active").length, icon: "checkmark-circle", color: "#16A34A" }, { label: "Subscribed", value: parents.filter((p) => p.fields?.some((f) => f.includes("active"))).length, icon: "card", color: "#DB2777" }, { label: "Blocked", value: parents.filter((p) => p.status === "blocked").length, icon: "ban", color: "#DC2626" }]} filters={["All", "Active", "Blocked", "Inactive"]} searchPlaceholder="Parent, mobile, child or school" actionLabel="Add parent" />; }
