import React from "react";
import { EntityPage } from "./pagekit";
import { drivers, SCHOOL_NAMES } from "./mockData";
export default function DriversPage({ onNavigate }: { onNavigate?: (page: string) => void }) { return <EntityPage title="Drivers" subtitle="Verification, license, assigned bus, route and account status." seed={drivers} schoolNames={SCHOOL_NAMES} onNavigate={onNavigate} metrics={[{ label: "Total drivers", value: drivers.length, icon: "people", color: "#EA580C" }, { label: "Active", value: drivers.filter((d) => d.status === "active").length, icon: "checkmark-circle", color: "#16A34A" }, { label: "Pending verification", value: drivers.filter((d) => d.status === "inactive").length, icon: "time", color: "#EA580C" }, { label: "Blocked", value: drivers.filter((d) => d.status === "blocked").length, icon: "ban", color: "#DC2626" }]} filters={["All", "Active", "Inactive", "Blocked", "Pending"]} searchPlaceholder="Driver, mobile, school or bus" actionLabel="Add driver" />; }
