import React from "react";
import { EntityPage } from "./pagekit";
import { payments, SCHOOL_NAMES } from "./mockData";
const withdrawals = payments.filter((item) => item.title.startsWith("WD-"));
export default function WithdrawalRequestsPage() { return <EntityPage workflow="payment" title="School withdrawal requests" subtitle="Review bank information, available balance, notes and payout status." seed={withdrawals} schoolNames={SCHOOL_NAMES} metrics={[{ label: "Total requests", value: withdrawals.length, icon: "wallet", color: "#EA580C" }, { label: "Pending", value: withdrawals.filter((w) => w.status === "pending").length, icon: "time", color: "#EA580C" }, { label: "Processing", value: withdrawals.filter((w) => w.status === "processing").length, icon: "sync", color: "#2563EB" }, { label: "Total amount", value: "₹49,200", icon: "cash", color: "#0F766E" }]} filters={["All", "Pending", "Processing", "Completed", "Rejected"]} searchPlaceholder="Withdrawal ID, school or bank reference" actionLabel="Refresh withdrawals" />; }
