import React, { useState } from "react";
import { Pressable, Text, TextInput, View } from "react-native";
import { AdminPageFrame, styles } from "./pagekit";
export default function NotificationsPage() { const [message, setMessage] = useState(""); const [sent, setSent] = useState<string[]>([]); return <AdminPageFrame title="Notifications" subtitle="Send parent, driver, school and emergency notifications."><View style={styles.formCard}><Text style={styles.formLabel}>Recipient type</Text><View style={styles.recipientRow}>{["Everyone", "Parents", "Drivers", "Schools"].map((item) => <View key={item} style={styles.recipient}><Text style={styles.recipientText}>{item}</Text></View>)}</View><Text style={styles.formLabel}>Title</Text><TextInput placeholder="Bus service update" placeholderTextColor="#98A2B3" style={styles.formInput} /><Text style={styles.formLabel}>Message</Text><TextInput multiline value={message} onChangeText={setMessage} placeholder="Write notification..." placeholderTextColor="#98A2B3" style={styles.formArea} /><Pressable onPress={() => { if (message.trim()) { setSent((items) => [message.trim(), ...items]); setMessage(""); } }} style={styles.action}><Text style={styles.actionText}>Send notification</Text></Pressable></View><Text style={styles.sectionTitle}>Notification history</Text>{sent.map((item, index) => <View key={`${item}-${index}`} style={styles.card}><Text style={styles.cardTitle}>Broadcast sent</Text><Text style={styles.cardSubtitle}>{item}</Text></View>)}</AdminPageFrame>; }
